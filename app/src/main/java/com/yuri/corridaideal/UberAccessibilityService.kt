package com.yuri.corridaideal

/**
 * Mantido apenas como stub de compatibilidade para sobrescrever a implementação
 * experimental da v0.6.0 em repositórios que já a possuíam.
 * A v1.0.0 NÃO usa AccessibilityService.
 */
class UberAccessibilityService
