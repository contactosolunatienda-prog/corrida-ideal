package com.yuri.corridaideal

/**
 * Legacy filename kept only so a web upload can overwrite the old v0.5.1 source.
 * v0.5.2 does NOT use Android AccessibilityService and this object is not declared
 * in AndroidManifest.xml.
 */
object UberAccessibilityService
